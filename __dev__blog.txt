14-07-2025 to 24-07-2025:
  ->Experimenting with building own file systems and learn about ata command ports and about 32 bit mode.
  ->Planning the course of the project and setting temporary milestones.

25-07-2025:
  ->Starting the actual project, Booted the OS and Entered the 32 bit Protected Mode.
  ->Made 2 Helper functions like print(), clear_screen();

26-07-2025:
  ->made many helper functions and functions like mem which is equivalent of malloc which made the development process easy;
  ->made a ram based custom file-system(inlcuding directories) with tree like custom designed structure.
  ->have options like create,delete,list,write,read.etc...

27-07-2025:
  ->since the ram based system is not persistent ,tried to make it disk based but failed.
  ->rewritten the entire code to make it disk based and written few extra helper functions too.
  ->the rewritten code is in such a way that i dont have to rewrite it in order to make updates again.

Critical Challenge:
 ->converting entire code into disk.(27-07-25)

28-07-2025 to 02-08-2025:
  ->Made a cli to make it a functioning os from user perspective.
  ->Extended the disk space and changed the structure of the disk to ease future development.
  ->Made a Basic Authentication system

Challenge:
 ->there was corruption of disk file due to bug in code which rewrites the kernel code(01-08-2025).
 ->Fixed it by properly seperating the kernel sectors,file entry sectors and data sectors.

2-08-2025 - 17-08-2025:

 ->experimenting about networking in a distributive system and the internet.
 -> added copy , move , paste command.

18-11-2025:

  ->upgraded the goto and pos functions to work with path as inputs instead of one step input
<<<<<<< Updated upstream
=======
  ->upgraded the find_dir and find_file functions from vauge sequencial search to a subtree like search

19-11-2025:

  ->converted the single-level input file system to a path based input file system

Challenge:
  ->the 20kb of memory which is loaded by the bootloader has overflown, 
  ->found and fixed my incrementing it to 50kb of memory to be stable
   
>>>>>>> Stashed changes
